<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Required for push notifications on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- SC-179: freeRASP 8 registers screenshot and screen-recording detectors
         at startup and logs a TalsecScreenProtector failure for each without
         these. They are normal (install-time) permissions with no runtime
         prompt, and they only let the app be TOLD that a capture happened —
         they grant no access to screen content, and nothing is collected or
         transmitted beyond the threat's name. Declared with maxSdkVersion
         omitted deliberately: both are no-ops on older releases. -->
    <uses-permission android:name="android.permission.DETECT_SCREEN_CAPTURE"/>
    <uses-permission android:name="android.permission.DETECT_SCREEN_RECORDING"/>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config">
        
        <meta-data android:name="com.google.android.geo.API_KEY"
               android:value="@string/google_maps_key"/>
            

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Android App Links (TAB 14).

                 The app had no deep links of its own: one launcher filter, and
                 one browsable filter belonging to the Facebook login redirect.
                 Email and push links could not open the app at all, which caps
                 the value of every notification and blocks password reset from
                 completing in-app.

                 `autoVerify="true"` makes Android fetch
                 https://<host>/.well-known/assetlinks.json and check this app's
                 release signing certificate against it. Until that file is
                 hosted with the RELEASE fingerprint, verification FAILS and
                 these links open in the browser — which is the safe direction,
                 and why shipping this before the file exists costs nothing.

                 Paths are enumerated rather than wildcarded so the app claims
                 exactly what DeepLinkResolver.claimedPrefixes resolves. A
                 claimed path the resolver returns null for opens the app to
                 nothing. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="servana.com.ph" />
                <data android:scheme="https" android:host="www.servana.com.ph" />
                <data android:scheme="https" android:host="app.servana.com.ph" />
                <data android:pathPrefix="/bookings" />
                <data android:pathPrefix="/conversations" />
                <data android:pathPrefix="/notifications" />
                <data android:pathPrefix="/services" />
                <data android:pathPrefix="/reset-password" />
            </intent-filter>
        </activity>

          <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <!-- FCM: default notification channel icon and color -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="servana_default" />

        <!-- Facebook Sign-In: replace string resource values in res/values/strings.xml
             after creating a Facebook App at developers.facebook.com and enabling
             Facebook in Firebase Console > Authentication > Sign-in method -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token"/>

        <!-- Facebook SDK: required activities and content provider for Android login redirect -->
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />

        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <!-- Replace REPLACE_WITH_FB_APP_ID below with the numeric Facebook App ID
             from developers.facebook.com (must match facebook_app_id in strings.xml) -->
        <provider
            android:name="com.facebook.FacebookContentProvider"
            android:authorities="com.facebook.app.FacebookContentProvider2048115409245680"
            android:exported="true" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility?hl=en and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
